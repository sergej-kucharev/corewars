const smallest = (N) => {
    const swap = (s, i, j) => +[...s].splice(i, 1).splice(j, 0, s[i]).join('');
    const s = []+N, l = s.length;
    for (const i=0; i<s.length; i++) {

    }
    return [...s].reduce((I, n, i) => {}, []);
};